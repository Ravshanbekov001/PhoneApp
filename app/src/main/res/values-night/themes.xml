<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PhoneApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:navigationBarColor">@color/black</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="cardBackground">
        <item name="cardBackgroundColor">@color/card_night_color</item>
    </style>

    <style name="MaintextColor">
        <item name="android:textColor">@color/white</item>
    </style>

    <color name="textColor">#FFFFFF</color>

    <style name="textColor">
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="edtBackground">
        <item name="android:background">@color/card_night_color</item>
    </style>

    <style name="edtCardBackground">
        <item name="cardBackgroundColor">@color/card_night_color</item>
    </style>

    <style name="AddCardBackground">
        <item name="cardBackgroundColor">@color/card_night_color</item>
    </style>

</resources>